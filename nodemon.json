{
  "watch": ["server"],
  "ext": "ts,json",
  "ignore": ["dist", "cypress", "client"],
  "exec": "rimraf dist/server && yarn build:server && node dist/server/index.js",
  "events": {
    "start": "kill-port 3002"
  }
}

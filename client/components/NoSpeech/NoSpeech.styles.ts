import styled from 'styled-components';

export const NoSpeechWrapper = styled.div`
  width: 100vw;
  padding: var(--medium);
  background-color: var(--error);
  text-align: center;

  * {
    color: var(--core-white) !important;
  }
`;

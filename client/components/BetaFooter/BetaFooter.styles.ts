import styled from 'styled-components';

export const BetaFooterWrapper = styled.div`
  padding: var(--medium);
  text-align: center;
  background-color: var(--success);

  * {
    color: var(--core-white);
  }
`;
